## Commands
``npm run gpx`` will generate a new ``gpx-index.json`` manifest file from the GPX files you place in subfolders within ``/gpx/``

``npm run gpx:watch`` does the same as above, but runs a worker to monitor for changes to the ``/gpx`` folder - great to quickly see new files.

``npx serve .`` to start your local server and render the map.
